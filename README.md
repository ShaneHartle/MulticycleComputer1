# MulticycleComputer1
This is a custom built 36-bit multicycle CPU built with Harvard Architecture. The project was made with System Verilog and was simulated in EDA Playground using Aldec Riviera Pro. The CPU used reverse endian logic, and includes an instruction memory, data memory, ALU, register file, and a control unit FSM. The bench tests an addition and for loop.
